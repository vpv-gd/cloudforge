%YAML 1.2
--- # configuration file (installation parameters) for cloudforge
chef:
    server: chef-s-a
    key: /path/to/key

cloud:
    provider: gogrid
    key: alpha
    secret: beta

db:
    engine: sqlite3
    file: /path/to/sqlite.db

ssh:
    node_key: /path/to/node.key
    forge_key: /path/to/forge.key

# installation-specific app-specific parameter
navapp:
    SCENE7_IMG_SERVER: http://lala.la/la-la/
    
...
